{
  "config": {
    "step": {
      "user": {
        "title": "Set up BlueSight",
        "description": "BlueSight reads your existing Bluetooth proxies and needs no configuration to start. Storm and poll settings can be tuned later in the integration's options."
      }
    },
    "abort": {
      "single_instance_allowed": "Only a single instance of BlueSight is allowed. There is one Bluetooth stack per Home Assistant."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "BlueSight options",
        "description": "Tune the pairing-storm window, the poll backstop, and the proxy-health thresholds.",
        "data": {
          "storm_window_s": "Storm window (seconds)",
          "storm_threshold": "Storm threshold (failures)",
          "poll_interval_s": "Poll interval (seconds)",
          "stalled_threshold_s": "Stalled threshold (seconds)",
          "reboot_window_s": "Reboot window (seconds)",
          "reboot_threshold": "Reboot threshold (reboots)"
        },
        "data_description": {
          "storm_window_s": "Sliding window over which repeated connection failures are counted.",
          "storm_threshold": "Number of failures within the window that raises a pairing-storm incident.",
          "poll_interval_s": "How often the slot snapshot is refreshed as a backstop to push updates.",
          "stalled_threshold_s": "How long a proxy can go without any slot activity before it is flagged as stalled.",
          "reboot_window_s": "Sliding window over which proxy reboots are counted.",
          "reboot_threshold": "Number of reboots within the window that raises a flapping-proxy incident."
        }
      }
    }
  }
}
