[tool.pytest.ini_options]
# Run pytest from the repo root; make the repo importable so that
# `custom_components.ble_triage...` resolves without installing the package.
pythonpath = ["."]
testpaths = ["tests"]
